spring:
  application:
    name: gateway-service
  cloud:
    consul:
      host: localhost
      config:
        format: YAML
      discovery:
        register-health-check: false

    loadbalancer:
      ribbon:
        enabled: false

server.port: 8080