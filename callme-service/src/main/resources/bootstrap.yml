spring:
  application:
    name: callme-service
  cloud:
    consul:
      host: localhost
      discovery:
        hostname: localhost
        register-health-check: false
  config:
    import: optional:consul:localhost:8500